CREATE OR REPLACE PACKAGE USUARIOS_PKG AS

    --Crea un nuevo usuario.
    PROCEDURE CREAR_USUARIO_PR(P_ID NUMBER, P_NOMBRE VARCHAR2, P_CONTRASENA VARCHAR2);

    --Autentica un usuario para iniciar sesión.
    PROCEDURE AUTENTICAR_USUARIO_PR(P_ID NUMBER, P_CONTRASENA VARCHAR2);

    --Actualiza la puntuación de los jugadores.
    PROCEDURE ACTUALIZAR_PUNTUACION_PR(P_ID_GANADOR NUMBER, P_ID_PERDEDOR NUMBER);

    --Consulta las estadísticas de un jugador.
    PROCEDURE CONSULTAR_JUGADOR_PR(P_ID_JUGADOR IN OUT NUMBER, PARTIDAS_JUGADAS OUT NUMBER, PARTIDAS_GANADAS OUT NUMBER,
                                    PARTIDAS_PERDIDAS OUT NUMBER);

END USUARIOS_PKG;

